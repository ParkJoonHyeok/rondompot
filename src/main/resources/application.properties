spring.application.name=randompot-api
server.port=${PORT:8080}

# Comma-separated origins. Keep localhost for local dev.
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000}

# Keep health endpoint available for platform health checks.
management.endpoints.web.exposure.include=health
